# TEMPORARY - UID2-7820
# Tests the shared-increase-version-number.yaml fix (skip package-lock.json for
# yarn-only repos) against its feature branch before it's merged to @v3.
# Delete this file once sra-UID2-7820-skip-package-lock-for-yarn is merged.
name: TEMP - Test Shared Version Bump (UID2-7820)
run-name: TEMP test version bump shared action by @${{ github.actor }}
on:
  push:
    branches-ignore:
      - main

jobs:
  incrementVersionNumber:
    uses: IABTechLab/uid2-shared-actions/.github/workflows/shared-increase-version-number.yaml@sra-UID2-7820-skip-package-lock-for-yarn
    with:
      release_type: Snapshot
    secrets: inherit
